Create a 16:9 Chinese presentation slide.
Headline: YAML 先定义“评什么”，并规定判断顺序
Sub-headline: 先把评价对象和程序写清，模型才不会直接跳到结论
Body:
- precheck 决定论文能否进入六维评分
- dimensions + weight 定义六维结构与加权关系
- decision_order 规定每个维度必须先看什么、后看什么
Visual: two-column slide showing YAML objects and runtime effects.
