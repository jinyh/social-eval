x-logging: &default-logging
  driver: json-file
  options:
    max-size: "20m"
    max-file: "5"

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-socialeval}
      POSTGRES_USER: ${POSTGRES_USER:-socialeval}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
    volumes:
      - ${SOCIALEVAL_DATA_ROOT:?set SOCIALEVAL_DATA_ROOT}/postgres:/var/lib/postgresql/data
    cpus: 2
    mem_limit: 8g
    logging: *default-logging
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U "$${POSTGRES_USER}" -d "$${POSTGRES_DB}"
      interval: 10s
      timeout: 5s
      retries: 10
    security_opt:
      - no-new-privileges:true

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - ${SOCIALEVAL_DATA_ROOT:?set SOCIALEVAL_DATA_ROOT}/redis:/data
    cpus: 1
    mem_limit: 2g
    logging: *default-logging
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    security_opt:
      - no-new-privileges:true

  migrate:
    build:
      context: .
      dockerfile: Dockerfile.api
    env_file:
      - ${ENV_FILE:-.env.production}
    environment:
      APP_ENV: production
      DATABASE_URL: ${DATABASE_URL:?set DATABASE_URL}
      REDIS_URL: redis://redis:6379/0
    command: ["alembic", "upgrade", "head"]
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    cpus: 1
    mem_limit: 2g
    logging: *default-logging
    security_opt:
      - no-new-privileges:true

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    restart: unless-stopped
    init: true
    env_file:
      - ${ENV_FILE:-.env.production}
    environment:
      APP_ENV: production
      DATABASE_URL: ${DATABASE_URL:?set DATABASE_URL}
      REDIS_URL: redis://redis:6379/0
      SESSION_HTTPS_ONLY: "true"
      ADMIN_MFA_REQUIRED: "true"
    volumes:
      - ${SOCIALEVAL_DATA_ROOT:?set SOCIALEVAL_DATA_ROOT}/app:/app/data
    command:
      - uvicorn
      - src.api.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --proxy-headers
      - --forwarded-allow-ips=*
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import os, urllib.request;
          host = next((value.strip() for value in
          os.getenv('ALLOWED_HOSTS', '').split(',') if value.strip()),
          '127.0.0.1');
          request = urllib.request.Request(
          'http://127.0.0.1:8000/api/health/ready', headers={'Host': host});
          urllib.request.urlopen(request)
      interval: 20s
      timeout: 5s
      retries: 5
    security_opt:
      - no-new-privileges:true
    cpus: 2
    mem_limit: 4g
    logging: *default-logging

  worker:
    build:
      context: .
      dockerfile: Dockerfile.api
    restart: unless-stopped
    init: true
    env_file:
      - ${ENV_FILE:-.env.production}
    environment:
      APP_ENV: production
      DATABASE_URL: ${DATABASE_URL:?set DATABASE_URL}
      REDIS_URL: redis://redis:6379/0
    volumes:
      - ${SOCIALEVAL_DATA_ROOT:?set SOCIALEVAL_DATA_ROOT}/app:/app/data
    command:
      - celery
      - -A
      - src.tasks.celery_app.celery_app
      - worker
      - --loglevel=INFO
      - --concurrency=3
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - celery -A src.tasks.celery_app.celery_app inspect ping
          --destination "celery@$${HOSTNAME}" --timeout 5
      interval: 30s
      timeout: 10s
      retries: 5
    security_opt:
      - no-new-privileges:true
    cpus: 4
    mem_limit: 16g
    logging: *default-logging

  frontend:
    build:
      context: src/web
      dockerfile: Dockerfile
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cpus: 0.5
    mem_limit: 512m
    logging: *default-logging

  proxy:
    image: caddy:2.10-alpine
    restart: unless-stopped
    environment:
      APP_DOMAIN: ${APP_DOMAIN:-localhost}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ${SOCIALEVAL_DATA_ROOT:?set SOCIALEVAL_DATA_ROOT}/caddy/data:/data
      - ${SOCIALEVAL_DATA_ROOT:?set SOCIALEVAL_DATA_ROOT}/caddy/config:/config
    depends_on:
      api:
        condition: service_healthy
      frontend:
        condition: service_started
    security_opt:
      - no-new-privileges:true
    cpus: 0.5
    mem_limit: 512m
    logging: *default-logging
